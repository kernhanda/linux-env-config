-- Plugin configurations have been split into separate files:
--   flash.lua        - Flash.nvim (fast navigation)
--   lsp.lua          - LSP configuration (clangd, mojo)
--   treesitter.lua   - Treesitter syntax highlighting
--   dap.lua          - Debug Adapter Protocol
--   cursor-agent.lua - Cursor Agent integration
--
-- Add new plugin configurations in their own files.

return {}
