-- Neovim's default :bdelete command can be quite annoying,
-- since it also messes up your entire window layout by
-- deleting windows. bufdelete.nvim aims to fix that by
-- providing useful commands that allow you to delete a buffer
-- without messing up your window layout.
return {
	"famiu/bufdelete.nvim",
}
